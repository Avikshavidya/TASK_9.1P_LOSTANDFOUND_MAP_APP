<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="LostAndFoundApp"
        android:supportsRtl="true"
        android:theme="@style/Theme.TASK_91P_LOST_AND_FOUND_APP">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC_yqv-CuJNPrGHTdASuzlpM7nlOtFSqSA" />

        <activity android:name=".MapActivity" />
        <activity android:name=".ItemDetailActivity" />
        <activity android:name=".ItemsListActivity" />
        <activity android:name=".CreateAdvertActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>